<Window x:Class="Convert3DObject._3DModelPreview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         xmlns:helix="http://helix-toolkit.org/wpf"
        xmlns:local="clr-namespace:Convert3DObject"
        mc:Ignorable="d"
        Title="_3DModelPreview" Height="450" Width="800">
    <Grid>
        <helix:HelixViewport3D  x:Name="viewPort3d" ZoomExtentsWhenLoaded="true" Margin="22,0,0,0" Height="366" VerticalAlignment="Center" HorizontalAlignment="Left" Width="511" Background="White" InfoBackground="#80FFFFFF" InfoForeground="{x:Null}" Panel.ZIndex="1" >
            <!-- Remember to add light to the scene -->
            <helix:DefaultLights/>
        </helix:HelixViewport3D>
        <Label Content="Label" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Height="390" Width="535" Background="Black"/>
    </Grid>
</Window>
